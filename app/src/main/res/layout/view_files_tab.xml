<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/color_background">

    <!-- Breadcrumb bar -->
    <LinearLayout
        android:id="@+id/files_breadcrumb_bar"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:background="@color/color_surface">

        <ImageButton
            android:id="@+id/files_btn_back"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_nav_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/color_text_primary"
            android:contentDescription="Back" />

        <ImageButton
            android:id="@+id/files_btn_home"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_home_dir"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/color_accent_primary"
            android:contentDescription="Home" />

        <TextView
            android:id="@+id/files_breadcrumb_path"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="~"
            android:textColor="@color/color_text_secondary"
            android:textSize="13sp"
            android:paddingStart="4dp"
            android:singleLine="true"
            android:ellipsize="start" />

        <ImageButton
            android:id="@+id/files_btn_search"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/ic_search"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:tint="@color/color_text_secondary"
            android:contentDescription="Search" />

    </LinearLayout>

    <!-- Action buttons: + File, + Folder -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:gravity="center_vertical">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/files_btn_new_file"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="@string/files_new_file"
            android:textColor="@color/color_text_primary"
            android:textSize="12sp"
            app:icon="@drawable/ic_add_file"
            app:iconTint="@color/color_text_primary"
            app:iconSize="16dp"
            app:iconPadding="4dp"
            app:rippleColor="@color/color_ripple"
            android:layout_marginEnd="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/files_btn_new_folder"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="@string/files_new_folder"
            android:textColor="@color/color_folder_accent"
            android:textSize="12sp"
            app:icon="@drawable/ic_add_folder"
            app:iconTint="@color/color_folder_accent"
            app:iconSize="16dp"
            app:iconPadding="4dp"
            app:rippleColor="@color/color_ripple" />

        <View android:layout_width="0dp" android:layout_height="1dp" android:layout_weight="1" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_border" />

    <!-- File list -->
    <ListView
        android:id="@+id/files_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:scrollbarStyle="outsideOverlay"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:clipToPadding="false" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/files_empty_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="@string/files_empty"
        android:textColor="@color/color_text_secondary"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>
